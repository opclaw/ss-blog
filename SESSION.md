# Session Log — Smart Solutions Site

## 2026-07-01 — Удаление Яндекс.Метрики со всех страниц

### What was done
- Пользователь попросил удалить Яндекс.Метрику со всех страниц сайта.
- Проверены все 5 HTML-страниц в `version 4/` на наличие кода метрики:
  - `index.html`
  - `services.html`
  - `cases/cnd-store.html`
  - `cases/moya-planeta.html`
  - `cases/arhgrille.html`
- Из каждой страницы удалён блок:
  - inline-скрипт `Yandex.Metrika counter`;
  - `noscript`-пиксель `mc.yandex.ru/watch/...`;
  - комментарии `<!-- Yandex.Metrika counter -->` и `<!-- /Yandex.Metrika counter -->`.
- Повторно проверено `grep`'ом по `version 4/`: следов `yandex.ru/metrika`, `mc.yandex.ru` и `ym(` не осталось.
- Проверено через Playwright (viewport 1512×982, локальный HTTP-сервер):
  - Все 5 страниц загружаются без ошибок в консоли.
  - Скрипты Яндекс.Метрики и inline-вызовы `ym()` отсутствуют.
  - Карточки контактов и остальной функционал на месте.
- Коммит `0152e8f`: `fix: удалён Яндекс.Метрика со всех страниц`.

- Актуальная версия `version 4/` выгружена на хостинг `smartsolutions.today` через SFTP/rsync.
- Проверка продакшена через Playwright (viewport 1512×982):
  - Все 5 страниц открываются, ошибок в консоли нет.
  - Яндекс.Метрика отсутствует на всех страницах (`ym_scripts=0`, `ym_inline=0`).
  - Cookie-баннер отсутствует.
  - Карточки контактов присутствуют по 4 штуки на каждой странице.

### Current state
- **Ветка:** `no-form-version`.
- **Последний коммит:** `e16e8fc` (2026-07-01) — удаление метрики + уборка служебного `.claude-docs`.
- **Рабочее дерево:** чистое.
- **Хостинг:** актуальная версия без метрики развёрнута.

### Next steps
- Проверить визуально на MacBook 16".
- По подтверждению — мердж `no-form-version` → `main`.

---

## 2026-06-30 — Версия без формы: финальная чистка

### What was done
- Продолжена работа в ветке `no-form-version`.
- Пользователь дал добро на удаление:
  - неиспользуемых стилей формы и куки-баннера;
  - правовых страниц и их ссылок в футере;
  - самого куки-баннера.
- Выполнена чистка:
  - Из `version 4/styles.css` удалены стили `.cta-buttons`, `.cta-form`, `.form-row`, `.form-floating`, `.form-input`, `.form-select`, `.form-textarea`, `.consent-label`, `.consent-checkbox`, `.consent-link`, `.btn-submit`, `.honeypot`, `.input-valid/invalid`, `.char-counter`, `.task-progress`, `.shake`, `.confetti-particle`, `.cookie-banner` и связанных.
  - Из `version 4/script.js` удалён весь блок формы (маска телефона, валидация, прогресс, отправка, конфетти). Остался только общий интерактив (прелоадер, навбар, аккордеон, плавная прокрутка и т.д.).
  - Удалены 4 правовые страницы:
    - `version 4/privacy-policy.html`
    - `version 4/privacy-policy-site.html`
    - `version 4/consent.html`
    - `version 4/consent-cookies.html`
  - Из футеров `index.html` и `services.html` убран блок `.footer-legal`.
  - Унифицированы футеры кейс-страниц:
    - `cases/cnd-store.html`
    - `cases/moya-planeta.html`
    - `cases/arhgrille.html`
    - Описание изменено на «Системы привлечения клиентов<br>на основе исследований».
    - Добавлена ссылка WhatsApp с иконкой 16px.
    - Иконка Telegram в подвале увеличена до 16px.
    - Убран блок `.footer-legal`.
- Проверено через Playwright (viewport 1512×982, локальный HTTP-сервер):
  - Все 5 страниц (`index.html`, `services.html`, `cases/cnd-store.html`, `cases/moya-planeta.html`, `cases/arhgrille.html`) загружаются без ошибок в консоли.
  - Cookie-баннер и `.footer-legal` отсутствуют.
  - `.contacts-grid` присутствует на всех страницах, 4 карточки в одном ряду.
  - Футер на кейс-страницах содержит WhatsApp.
  - Визуальные скриншоты CTA-блоков подтвердили ровное выравнивание подписей и значений.
- Коммит `ecff553`: `refactor: убрана форма и куки-баннер, унифицированы футеры кейсов`.

### Current state
- **Ветка:** `no-form-version`.
- **Последний коммит:** `ecff553` (2026-06-30) — финальная чистка.
- **Рабочее дерево:** чистое.
- **Локальный сервер:** остановлен.
- **Ожидает:** визуальной проверки пользователем на MacBook 16".

### Next steps
- Получить подтверждение, что карточки контактов выглядят ровно на MacBook 16".
- После подтверждения — мердж `no-form-version` → `main` и деплой на хостинг.

---

## 2026-06-30 — Версия без формы: аудит и исправление JS-ошибок

### What was done
- Продолжена работа в ветке `no-form-version`.
- Пользователь попросил провести аудит: убедиться, что нет лишнего кода и всё корректно работает.
- Аудит выявил критическую ошибку: после удаления формы `script.js` падал с ошибкой `Cannot read properties of null (reading 'addEventListener')` на всех страницах без формы (index, services, кейсы).
- В `version 4/script.js` все обработчики формы (маска телефона, валидация полей, отправка) обёрнуты в проверку существования формы и её полей.
- Проверено через Playwright на 5 страницах: `index.html`, `services.html`, `cases/cnd-store.html`, `cases/moya-planeta.html`, `cases/arhgrille.html`.
- Все страницы загружаются без ошибок в консоли, сетки контактов присутствуют, по 4 карточки в каждой.
- Коммит `48c1573`: `fix(script): защита от ошибок при отсутствии контактной формы`.

### Найденные несоответствия (не критичные, требуют решения)
- В CSS остались неиспользуемые стили `.cta-buttons` (бывшие кнопки CTA).
- В CSS осталась полная секция стилей формы `.cta-form`, `.form-row` и т.д., хотя HTML-формы больше нигде нет.
- В footer страниц кейсов:
  - нет WhatsApp (есть только Telegram);
  - иконка Telegram 14px, а на index/services — 16px;
  - `footer-desc` говорит «Разработка сайтов», а не «Системы привлечения клиентов».

### Current state
- **Ветка:** `no-form-version`.
- **Последний коммит:** `48c1573` — защита JS от ошибок при отсутствии формы.
- **Рабочее дерево:** чистое.
- **Локальный сервер:** остановлен.
- **Ожидает:** решения по найденным несоответствиям и визуальной проверки пользователем.

### Next steps
- Решить, удалять ли неиспользуемые стили формы и `.cta-buttons`.
- Решить, унифицировать ли footer на страницах кейсов (WhatsApp, размер иконки, текст описания).
- Получить подтверждение, что карточки контактов выглядят ровно на MacBook 16".
- После подтверждения — мердж `no-form-version` → `main` и деплой на хостинг.

---

## 2026-06-30 — Версия без формы: единообразные CTA на services и кейсах

### What was done
- Продолжена работа в ветке `no-form-version`.
- По обратной связи пользователь заметил две вещи:
  1. Кнопки «Обсудить» на `services.html` ведут на `index.html`, хотя внизу страницы есть CTA-блок.
  2. На страницах кейсов (`cases/*.html`) CTA содержит только телефон и Telegram, а не полный набор каналов.
- Решение:
  - `services.html`: секции CTA добавлен `id="contacts"`, кнопки «Обсудить» теперь ведут на `#contacts`.
  - `cases/cnd-store.html`, `cases/moya-planeta.html`, `cases/arhgrille.html`: сохранены заголовок и подзаголовок CTA, а блок «телефон + Telegram» заменён на `.contacts-grid` с 4 карточками (Позвонить, Telegram, WhatsApp, Email).
- Проверено через Playwright (viewport 1728×1117):
  - На `services.html` и всех 3 страницах кейсов карточки в одном ряду.
  - Подписи и значения на общих горизонтальных линиях.
  - Кнопки «Обсудить» имеют корректный `href="#contacts"`.
- Коммит `15fdb01`: `feat(cta): единообразные контакты на services и страницах кейсов`.

---

## 2026-06-30 — Версия без формы: крупнее текст в карточках контактов

### What was done
- Продолжена работа в ветке `no-form-version`.
- По обратной связи пользователь уточнил, что раньше переносились именно значения (телефон, никнейм, email), а не заголовки.
- Предложен вариант улучшения: расширить сетку и сделать текст крупнее для лучшей читаемости и иерархии.
- В `version 4/styles.css`:
  - `.cta-content` расширен с 800px до 960px.
  - `.contact-label` увеличен с 0.75rem до 0.8rem.
  - `.contact-value` увеличен с 0.8rem до 0.9rem.
  - Горизонтальный padding карточек возвращён до 0.75rem.
- Проверено через Playwright (viewport 1728×1117):
  - 4 карточки в одном ряду на `index.html` и `services.html`.
  - Все значения в одну строку, без переносов.
  - Подписи и значения на общих горизонтальных линиях.
  - Иконки сохранили прежний визуальный масштаб.
- Коммит `849d872`: `style(contact-cards): крупнее текст и шире сетка контактов`.

---

## 2026-06-30 — Версия без формы: выравнивание текста в карточках контактов

### What was done
- Продолжена работа в ветке `no-form-version`.
- Исправлено выравнивание подписей (`contact-label`) и значений (`contact-value`) в карточках контактов:
  - Иконки обёрнуты в `.contact-icon-wrap` с фиксированной высотой 36px и центрированием.
  - Убран `margin-bottom` у `.contact-icon`, перенесён на обёртку.
  - Карточкам добавлен `overflow: hidden`, уменьшен горизонтальный padding до 0.5rem.
- Изменения применены в `version 4/index.html`, `version 4/services.html` и `version 4/styles.css`.
- Проверено визуально через Playwright: на обеих страницах подписи и значения сидят на общих горизонтальных линиях.
- Удалены временные скриншоты и скрипты предыдущей сессии (`screenshot-labels.py`, `screenshots/`).
- Коммит `bff232c`: `fix(contact-cards): выравнивание иконок, подписей и значений по общим базовым линиям`.

---

## 2026-06-30 — Версия без формы: карточки контактов

### What was done
- Переключились на ветку `no-form-version`.
- В `version 4/icons.svg` добавлены символы `icon-whatsapp` и `icon-email`.
- В `version 4/index.html` и `version 4/services.html` заменена контактная форма на 4 карточки контактов:
  - Позвонить (`tel:+79250909500`);
  - Telegram (`https://t.me/olegkrechetov`);
  - WhatsApp (`https://wa.me/79250909500`);
  - Email (`mailto:ok@smartsolutions.today`).
- В `version 4/styles.css` добавлены стили `.contacts-grid` и `.contact-card` с адаптивностью (4 → 2 → 1 колонки).
- Запущен локальный сервер `python3 -m http.server 8000` и открыто в Chrome для предпросмотра:
  - `http://localhost:8000/index.html`
  - `http://localhost:8000/services.html`
- Коммит `0bf7763`: `feat(contacts): замена формы на карточки контактов для версии без формы`.

### Current state
- **Ветка:** `no-form-version`.
- **Последний коммит:** `ed4d117` (2026-06-30) — email увеличен до 34px для равенства размеров.
- **Рабочее дерево:** чистое.
- **Локальный сервер:** запущен на порту 8000.
- **Ожидает:** визуальной проверки пользователем.

#### Refinement по обратной связи (2026-06-30)
- Уменьшены отступы и шрифт значений; добавлено `white-space: nowrap` и `minmax(0, 1fr)`.
- Убран дубль телефона: в WhatsApp теперь значение «Написать» вместо повтора номера.
- Иконки уменьшены с 32 до 28 px.
- Коммит `31d900a`: `style(contacts): компактные карточки, убран дубль номера`.

#### Refinement по обратной связи #2 (2026-06-30)
- Иконка email переделана с `stroke` на `fill`, чтобы визуальный вес совпал с phone/Telegram.
- В footer `index.html` и `services.html` добавлена ссылка WhatsApp с иконкой.
- Коммит `7529ad1`: `fix(icons, footer): email fill-иконка, WhatsApp в подвале`.

#### Refinement по обратной связи #3 (2026-06-30)
- Проверено через Playwright-скриншоты: WhatsApp и email были визуально меньше phone/Telegram.
- `icons.svg`: WhatsApp заменён на логотип с фоном; email переделан в более плотный fill-конверт.
- В карточках: phone/Telegram 28px, WhatsApp 32px, email 34px — визуальный вес выровнен.
- В подвале: WhatsApp 16px против Telegram 14px.
- `.cta-content` расширен до 800px, `.contact-value` уменьшен до 0.8rem — все значения в одну строку без переносов.
- Коммит `f88d47c`: `fix(icons): выравнивание визуального масштаба иконок`.

#### Refinement по обратной связи #4 (2026-06-30)
- Проверено через Playwright-скриншоты: email был квадратным, Telegram в подвале меньше WhatsApp.
- `icons.svg`: email переделан в стройный outline-конверт с `stroke-width="2"`.
- В карточках: phone/email 32px, Telegram/WhatsApp 30px — все иконки одного визуального веса.
- В подвале: Telegram и WhatsApp обе 16px.
- Временные скриншоты и скрипт удалены после проверки.
- Коммит `0159efe`: `fix(icons): email не квадратный, иконки подвала выровнены`.

#### Refinement по обратной связи #5 (2026-06-30)
- Проверено через Playwright-скриншот с направляющей линией: email сидел выше центра остальных иконок.
- Добавлен CSS-класс `.contact-icon-email` с `transform: translateY(3px)`.
- Класс применён к иконке email в карточках `index.html` и `services.html`.
- Коммит `c5b1a92`: `fix(icons): выровнять email по высоте с остальными иконками`.

#### Refinement по обратной связи #6 (2026-06-30)
- Уточнение: пользователь имел в виду, что иконка email физически меньше по размеру/высоте, чем другие.
- Проверено через Playwright-скриншот с границами иконок: email действительно ниже по высоте.
- Иконка email в карточках увеличена с 32px до 34px — визуальная высота совпала с phone/WhatsApp.
- Убран лишний CSS-класс `.contact-icon-email`.
- Временные скриншоты и скрипты удалены после проверки.
- Коммит `ed4d117`: `fix(icons): email увеличен до 34px для равенства размеров`.

---

## 2026-06-30 — Email, локализация и сроки хранения в правовых страницах

### What was done
- Во всех 4 правовых страницах добавлен E-mail `ok@smartsolutions.today` в реквизитные блоки:
  - `privacy-policy.html`
  - `privacy-policy-site.html`
  - `consent.html`
  - `consent-cookies.html`
- В `privacy-policy.html` добавлены:
  - подраздел 10.1 «Локализация персональных данных»;
  - подраздел 10.2 «Сроки хранения».
- В `privacy-policy-site.html`:
  - раздел 10 переименован в «Сроки хранения»;
  - добавлен раздел 10.1 «Локализация персональных данных»;
  - добавлен раздел 11 «Защита информации» (бывшие «Меры безопасности»);
  - нумерация последующих разделов сдвинута.
- В `consent.html` пункт 5 дополнен: отзыв согласия возможен через e-mail `ok@smartsolutions.today`.
- В `consent-cookies.html` реквизитный блок структурирован с подписями «Оператор:» и «Контакты:».
- Коммит `30504e8`: `fix(legal): email, локализация и сроки хранения в правовых страницах`.
- Коммит `c55081f`: `fix(legal): вернуть раздел Защита информации в privacy-policy-site`.
- Коммит `1e22cf3`: `fix(legal): убран телефон из контактов privacy-policy-site`.
- Коммит `38cf3e5`: `fix(legal): email в правовых страницах — просто текст, не ссылка`.
- Все 4 файла выгружены на хостинг `smartsolutions.today`.
- Проверено через curl: изменения доступны на сайте, mailto-ссылок нет.

### Current state
- **Ветка:** `experiments`.
- **Последний коммит:** `38cf3e5` (2026-06-30) — email в правовых страницах как текст.
- **Рабочее дерево:** чистое.
- **Хостинг:** 4 правовые страницы синхронизированы с локальной `version 4/`.
- **Активная версия:** `version 4/`.

---

## 2026-06-30 — Обновление реквизитов в правовых страницах и на хостинге

### What was done
- Обновлены реквизиты ИП в 4 правовых страницах `version 4/`:
  - `privacy-policy.html`

## 2026-08-18 — AI-направление: лендинг ai.html (превью)

### What was done
- Ветка `ai-direction` от main (коммит 1167dde, запушена в origin).
- Создана `version 4/ai.html` — лендинг AI-направления в голосе сайта:
  hero «Вам не нужен ИИ. Вам нужны 20 часов команды в неделю», честно про ИИ (4 проблемы),
  4 услуги (база знаний, заявки, документы/КП, обучение), подход с метриками (1 процесс / 2–4 нед / 0 ₽ аудит),
  «Кому подходит / не подойдёт», FAQ (5), контакты через мессенджеры.
- `styles.css`: добавлен editorial-слой `.ai-*` — Manrope 800 с трекингом -0.03em,
  моно-номера секций, услуги строками (не карточки), без glow/градиентов.
- `script.js`: null-guards для preloader/scrollProgress/backToTop/cursorGlow
  (ai.html без этих элементов; поведение index.html не изменилось).
- Превью задеплоено на Vercel: https://ss-ai-preview.vercel.app/ai.html (команда opclaws-projects).

### Current state
- Прод (smartsolutions.today) НЕ тронут. main не менялся, работа в ветке ai-direction.
- Ожидает ревью пользователя по ссылке-превью.

### Next steps
- Правки по ревью → мердж ai-direction → main → rsync на Beget.
- Добавить ссылку «AI» в навигацию index.html/services.html (после одобрения).
- Обновить sitemap.xml при миграции.
- Дальше: блог-структура и вертикальные SEO-страницы (план из анализа detistov.ru).

## 2026-08-18 (вечер) — фикс контактов + спек-лист + решение проблемы Vercel BLOCKED

### What was done
- Починены контакты ai.html: системные классы (contacts-grid, contact-icon-wrap, contact-label/value, cta-*).
- Hero: двухколоночный layout + ai-spec — моноширинный «спек-лист аудита» с примером расчёта эффекта (часы/рубли).
- Диагностирована и решена проблема Vercel BLOCKED: CLI-деплой из git-рабочей копии
  несёт метаданные (githubOrg=crit-dmg, author=critdmg@gmail.com); Hobby-план требует
  авторство = член команды → блок. Решение: деплой из копии без .git (~/projects/ss-deploy).
- Новая версия в проде превью: https://ss-ai-preview.vercel.app/ai.html

### Deploy procedure (зафиксировано)
1. Правки в "version 4/" (ветка ai-direction)
2. rsync -a --exclude=.git,.claude,.DS_Store "version 4/" ~/projects/ss-deploy/
3. cd ~/projects/ss-deploy && vercel deploy --prod --yes --token=$VERCEL_TOKEN --scope opclaws-projects-1b852b6a
   (обязательно unset *_PROXY перед вызовом — socks5h конфликтует с CLI)

## 2026-08-20 — ai.html v2: концепция «инженерный документ»

### What was done
- Полная переработка ai.html по критике «недостаточно ново»:
  - hero-манифест «Нейросеть — это 20% результата. Остальные восемьдесят — процесс.»
  - серифный курсив Spectral italic как editorial-подпись (в системе шрифтов добавлен)
  - секция-формула «эффект = часы команды × ставка − стоимость внедрения»
  - протокол пилота по неделям (таймлайн 01–04)
  - секция «Мы откажем вам, если…» + «И наоборот»
  - ai-spec (спек-лист аудита) сохранён
- Деплой-процедура отлажена: rsync version 4/ → ~/projects/ss-deploy (без .git) → vercel deploy --prod.
  Причина: Vercel Hobby блокирует CLI-деплои с git-метаданными чужого аккаунта (crit-dmg ≠ opclaw team).
- Превью: https://ss-ai-preview.vercel.app/ai.html

### Next steps
- Ревью пользователем; правки.
- Если принято: ссылка AI в навигацию index/services, мердж → main, rsync на Beget, обновить sitemap.xml.

## 2026-08-22 — ai.html v3: система v4 + визуализации концептов (первый прогон оркестровки)

### What was done
- Решение пользователя: остаёмся в фирменной тёмной системе v4 (циан #00D9FF), но интегрируем находки из концептов (ai3: чертёж fig.01 с самоотрисовкой + бегущий пакет, квитанция аудита, формула эффекта, пилот с прогресс-полосами, отказы, моно-нумерация секций)
- Первый боевой прогон роутинга моделей: бриф на K3 → черновик субагент на ollama-launch/kimi-k2.7-code:cloud (115с, 46КБ) → QA K3 (15 структурных проверок + playwright, 0 JS-ошибок) → коммит
- Превью: https://ss-ai-preview.vercel.app/ai.html
- Деплой-процедура: rsync version 4/ → ~/projects/ss-deploy (без .git) → vercel deploy --prod

### Next steps
- Ревью пользователем; правки
- Раскрыть маппинг слепого теста (~/projects/bench-mapping.txt) после выбора пользователя
- После одобрения: ссылка AI в навигацию index/services, sitemap.xml, мердж → main, rsync на Beget

## 2026-08-22 — Первая pillar-статья блога (полный цикл роутинга)

### What was done
- CONTENT-MAP.md создан (карта тем: Детистов 64→61 уникальная × Wordstat × наши добавления)
- Первая статья P0: blog/vnedrenie-ii-v-biznes.html («Внедрение ИИ в бизнес», кластер 214/мес)
- Производство по MODEL-ROUTING.md: ТЗ K3 → черновик DeepSeek Pro (2220 слов, 12 H2, $0.005) → редактура K3 (2026, консистентность расчёта 7 мин) → сборка HTML
- Schema.org Article + FAQPage, canonical, OG; стили .article в styles.css
- Live-проверки: 0 JS-ошибок, 11 H2, FAQ×5, 2 schema-блока, 1786 слов контента
- Превью: https://ss-ai-preview.vercel.app/blog/vnedrenie-ii-v-biznes.html
- Playwright-нюанс: goto виснет на networkidle/domcontentloaded (медленный ресурс), работать с waitUntil:'commit'

### Next steps
- Ревью пользователем статьи и ai.html v3
- Слепой тест blind-1/2/3: раскрыть авторов после выбора
- Очередь P0: «ИИ для продаж» (307), «ИИ для маркетинга» (79)
- После одобрения: ai.html + блог в sitemap.xml, ссылка AI в навигацию, мердж → main → Beget

## 2026-08-23 — Связка сайта + дисциплина стиля + статья «ИИ для HR»

### What was done
- Статья «ИИ для HR» (blog/ii-dlya-hr.html): черновик ПОЛНОСТЬЮ на k2.7 Ollama (94с), редактура K3 — 3 точечные правки. Тест качества k2.7 пройден
- Фикс курсива: Spectral/italic убран из ai.html и styles.css — акценты теперь системные (Manrope 800 + циан), как на главной (там курсива нет)
- Связка меню: пункт «AI» добавлен в навигацию index.html, services.html, cases/* (десктоп + мобайл); blog/* — исправлены битые пути (../) — были 404
- ai.html: блок «Из блога» со ссылками на 4 статьи (ai-blog-list)
- Полный краул ссылок: 0 битых на 7 страницах
- Фикс подписи «листайте» (прилипала к тексту)
- SESSION: проверено, что .claude не утекает в git (снова проскочил — вычищен)

### Key decisions
- Курсив — не в стилистике сайта v4: акценты только цветом/весом Manrope. Правило зафиксировано
- Конвейер статей: assemble_article.py (скрипт) + фигуры K3 + черновик k2.7/DeepSeek

### Next steps
- blog.html — оглавление блога (4 статьи уже есть)
- Очередь P1: бухгалтерия (98), рестораны (52), недвижимость (49)
- Раскрыть маппинг слепого теста (~/projects/bench-mapping.txt)
- Мердж ai-direction → main + Beget (после одобрения пользователем)

## 2026-08-23 — P1-статьи конвейером (параллельная генерация)

### What was done
- 3 статьи P1 за один прогон: бухгалтерия (98), ресторан (52), недвижимость (49)
- Черновики: 3 субагента k2.7 параллельно (66–164 сек каждая, Ollama)
- Схемы K3: 6 SVG (первичка 6→1 мин, сверка с расхождением, контур отзывов, реактивация базы, скорость ответа 2 мин, подборка объектов)
- blog/index теперь 7 статей; ai.html блок «Из блога» — 7; sitemap — 16 URL
- Все live-проверки зелёные: 0 JS-ошибок, фигуры/FAQ/TL;DR/Schema на месте

### Экономика режима
- Полный цикл статьи: ~3 мин генерации на Ollama + схемы/приёмка K3. K3-трафик на статью снижен ~в 10 раз против ручного написания

## 2026-08-23 — Волна P2 на склад + E-E-A-T фиксы + слепой тест закрыт

### What was done
- Слепой тест завершён: победил DeepSeek Pro (дизайн-черновики → DeepSeek Pro, записано в MODEL-ROUTING.md)
- E-E-A-T фиксы: именной автор (Олег Кречетов, Person в Schema) + dateModified на 7 статей P0/P1
- Волна P2 (4 статьи на склад): онлайн-школы, медцентр, юристы, салон красоты
  - Черновики k2.7 параллельно; legal пришёл в yaml-формате — нормализован (урок: уточнять формат мета в ТЗ)
  - 8 SVG-схем K3, TL;DR, автор на всех
- blog/index: 11 статей, sitemap: 20 URL
- SEO-аудит перед запуском: canonical, title ≤60, schema — всё зелёное

### Ревизия скиллов (пользовательская договорённость)
- Прочитаны: statejnik, content-quality-auditor, seo-content, schema-markup-generator, meta-tags-optimizer, content-gap-analysis
- Забрано: E-E-A-T гейт-идеи (автор/источники/dateModified), скоринг тем — в конвейер
- Не подходит: seo-programmatic (масс-генерация), hreflang, local/maps, viral-formats

### Next steps
- ЗАПУСК В ПРОД — ждёт Beget-доступы (BEGET_HOST/USER/PASS в ~/.hermes/.env)
- После запуска: Яндекс.Вебмастер, публикация со склада 2-3/неделю
- Склад: 4 статьи P2 готовы к публикации

## 2026-09-25 — сайт перенесён на Astro (`site/`)
- Источник: `version 4.zip` → `legacy/version-4/` (эталон). Новый проект: `site/` (Astro, 27 страниц, те же адреса *.html).
- Шапка, мобильное меню, подвал, карточки контактов, Метрика — компоненты; контакты и меню — `site/src/data/site.ts`.
- `npm run check` (в `site/`): сборка + сверка с оригиналом + проверка JS. Итог: 27/27 OK, 0 битых ссылок.
- Исправлено: tel-ссылки (66), «Контакты» в подвале, id="contacts", незакрытый div в 20 статьях, мобильное меню «Как посчитать», canonical/og:url, иконки.
- Деплой теперь: залить `site/dist/`. Отчёт: `deliverables/SITE-MIGRATION-REPORT.md`.

## 2026-09-25 (второй прогон) — проверка фактов, правки, аудит и план команды

### What was done
- Прогнаны все проверки: `npm run check` в `site/` (сборка 28 страниц, сверка 27 с эталоном — OK,
  битых ссылок 0, JS и интерактив без ошибок). Рабочее дерево было чистым, зависших правок не нашлось.
- **Написан аудит сборки `site/tools/seo-audit.py`** (17 проверок: title/description и их длины, дубли между
  страницами, canonical/og, H1 и порядок заголовков, JSON-LD + связь FAQPage с видимым FAQ, автор и даты,
  alt у картинок, смешанный контент, вес, robots/sitemap/.htaccess, оглавление блога, перелинковка).
  Запуск — `npm run audit`; добавлен в `npm run check` и в CI. Итог: **0 ошибок, 17 предупреждений**.
- **Исправлено в сборке:**
  - `robots.txt`: убраны устаревший `Host`, правила для несуществующих `consent*.html` и `/*.json$`
    (закрывал `manifest.json`); добавлены явные разрешения ИИ-краулерам (GPTBot, ClaudeBot, PerplexityBot,
    OAI-SearchBot, ChatGPT-User, Claude-SearchBot, Google-Extended) и `Clean-param` для Яндекса.
  - `.htaccess`: правило против старых `/?p=123` не работало (`RewriteRule` не видит query string) —
    заменено на `RewriteCond %{QUERY_STRING}`; `manifest.json` выведен из-под запрета `.json`.
  - `sitemap.xml` больше не ведётся руками: `tools/make-sitemap.py` собирает карту из `dist`, `lastmod`
    берётся из `dateModified` статей (22 из 28 адресов). В `npm run check` добавлена проверка актуальности.
  - `ai.html`: не было карточки статьи «Что такое ИИ-агент», счётчик отраслей показывал (11) при 12 карточках.
  - Статья «ИИ для продаж»: блок TL;DR назывался «Основное» (единственная из 21) → «Коротко».
  - Картинки `public/images`: 6,0 МБ → 0,5 МБ (уменьшены до 700 px, перекодированы; в галерее выводятся 220×150).
    Эталонные оригиналы остаются в `legacy/version-4/images/` — откат возможен.
- **Доработан `tools/verify.py`**: механизм намеренных правок (`content-edits.json`) теперь понимает
  `title`, `meta`, `jsonld`, `text`, `links`, `classes`, `id`, `rewrite` — раньше работали только `text` и `rewrite`,
  из-за чего правки в ai.html показывались как расхождения.
- **Документация приведена к фактам:**
  - `CLAUDE.md`: структура (активная версия — `site/`, легаси — эталон), стек (Astro, Метрика стоит),
    статус прод-выгрузки, запреты, приоритеты, отсутствие `MODEL-ROUTING.md` (правило про него поправлено).
  - `CONTENT-MAP.md`: все 10 тем первой очереди помечены как опубликованные (были ⬜ при 21 статье на сайте),
    исправлено «без Метрики» (стоит на всех страницах), добавлен раздел 7 с очередью тем.
  - `site/README.md`: состав сборки (28 страниц), новые инструменты, команды `audit`/`sitemap`, порядок
    добавления статьи.
  - `deliverables/SITE-MIGRATION-REPORT.md`: раздел «Проверка 2026-09-25 (второй прогон)» — что в отчёте
    устарело (27→28 страниц, 20→21 статья, 60→64 файла в public), статус замечаний критиков.
- **Создан план** `deliverables/NEXT-STEPS-2026-09.md`: роли и находки, блокеры, план
  (сутки → неделя → месяцы), решения владельца с рекомендациями (в т.ч. 9 готовых заголовков ≤60 символов),
  метрики и контрольные точки, список «что не делаем».

### Найденные расхождения, которые НЕ исправлены (нужны решения)
1. Токен Telegram-бота остаётся в истории git (`version 4.zip`, коммит b3d783c) — отозвать в @BotFather.
2. На хостинге Beget до сих пор старая сборка `version 4/` — прод-выгрузка `site/dist/` не сделана.
3. Схемы нет на 5 страницах: главная, услуги, AI-хаб, 3 кейса.
4. 9 заголовков длиннее 60 символов, 1 description 186 символов (готовые формулировки — в плане).
5. 3 статьи без входящих ссылок: «ИИ для HR», «ИИ для медцентра», «ИИ для недвижимости».
6. Шрифты грузятся с Google Fonts; пункта «Блог» нет в шапке (статьи подсвечивают «AI»).
7. `MODEL-ROUTING.md` отсутствует в репозитории, хотя на него ссылаются `CLAUDE.md` и `CONTENT-MAP.md`.
8. Нет страницы 404 (`ErrorDocument` не настроен) — Beget отдаёт свою заглушку.
9. Метрика стоит на всех страницах, но цели (клики в мессенджеры) не настроены — заявки не считаются.

### Current state
- **Ветка:** `arena/01a0d848-ss-blog` (от `88bec6a`, ветка `main`).
- **Проверки:** `npm run check` — зелёный; `npm run audit` — 0 ошибок, 17 предупреждений (перечень выше).
- **Прод:** не обновлён (см. п. 2).

### Next steps
- Решения владельца по разделам 4 и 6 плана `deliverables/NEXT-STEPS-2026-09.md` (блокеры и 9 заголовков).
- Прод-выгрузка + Вебмастер + цели Метрики (сутки-неделя плана).
- Микроразметка на 5 страницах, 404, шрифты своего хостинга, перелинковка 3 статей (неделя плана).
- Дальше — переписывание 19 старых статей на макет `Article.astro` и новые темы из `CONTENT-MAP.md`, раздел 7.

## 2026-09-25 (третий прогон) — раунд 2: доступность, целостность разметки, контент-аудит

### What was done
- **Два новых инструмента:**
  - `site/tools/qa-check.py` (`npm run qa`): дубли `id`, все якоря (включая символы спрайта `icons.svg`),
    незакрытые теги, цель пропуска к содержимому, пустые ссылки/кнопки, alt, `<main>`, `aria-expanded`
    у бургера, совпадение контактов (тел/WhatsApp/Telegram/почта) со `src/data/site.ts`, типографика.
  - `site/tools/content-audit.py` (`npm run content`): объём и заявленное время чтения (180 слов/мин),
    однородность структуры (H2), дословные повторы абзацев, цифры и пометки о происхождении,
    путь к заявке из статьи, даты. Машинный отчёт — `tools/content-report.json` (в git не попадает).
- **Исправлено:**
  - `Base.astro`: содержимое обёрнуто в `<main>` (проп `mainWrap`, `Article.astro` его выключает) —
    ориентира не было ни на одной старой странице.
  - `Nav.astro`: логотип на главной вёл на `href="#"` → теперь всегда `/` + `aria-label`.
  - `.htaccess`: 301-редиректы для дублей `/index.html` → `/` и `/blog/index.html` → `/blog/`.
  - Время чтения приведено к факту: «Салон красоты» 9 → 13 мин (2297 слов), «Внедрение ИИ» 10 → 14 мин (2600 слов).
  - `seo-audit.py`: FAQ считается видимым и в формате «Частые вопросы + H3»; добавлены проверки
    валидности `manifest.json`/`browserconfig.xml`/`sitemap.xml`, наличия `og:image`, совпадения id Метрики,
    веса страниц (HTML + свои css/js + картинки).
  - `npm run check` теперь включает и QA-проверку; CI обновлён.
- **Проверено и в порядке:** дубли id — 0; незакрытые теги — 0; битые якоря — 0; контакты совпадают
  на всех 28 страницах; типографика чистая (кроме сознательно исключённых `<pre>` с промптами);
  у всех 21 статьи есть «Коротко», вопросы, 6–9 ссылок на услуги/AI, 11–12 контактных ссылок;
  дословных повторов абзацев между статьями — 0; `manifest.json` — валидный JSON, XML-файлы парсятся;
  главная 656 КБ вместе с картинками (было ~6,5 МБ), статьи 167–198 КБ, `kit.css` — только на статье
  с интерактивом.
- **Замеры по контенту:** объём статей 1304–2600 слов (в среднем 1990); в 19 из 21 статьи ровно 13–14
  разделов H2 — структурная однородность («одна статья с подставленной отраслью») подтверждена цифрами;
  439 единиц «%»/«раз»/«руб» и 254 пометки «оценка/пример/источник»; статей без пометок при ≥8 цифрах — 0.
- **План обновлён:** `deliverables/NEXT-STEPS-2026-09.md` дополнен разделами 8–11 (роли и находки раунда 2,
  четыре новые задачи: однородность статей, проверка `.htaccess` на хостинге, страница 404, каннибализация
  «продажи ↔ ИИ-агент», усиление заголовков; обновлённый порядок работ и список инструментов).

### Current state
- **Ветка:** `arena/01a0d848-ss-blog`. `npm run check` — зелёный (сверка OK, JS без ошибок, интерактив OK,
  QA 0 предупреждений, SEO-аудит 0 ошибок / 17 предупреждений).

### Next steps
- Блокеры владельца (токен бота, доступы Beget) → выгрузка → Вебмастер → цели Метрики.
- Мелкие технические задачи: 404, шрифты своего хостинга, микроразметка на 5 страницах.
- Перелинковка трёх статей и развязка каннибализации «ИИ для продаж» ↔ «Что такое ИИ-агент».
- Применение 9 готовых заголовков ≤60 символов; дальше — переписывание статей на `Article.astro`.

## 2026-09-25 (четвёртый прогон) — раунд 3: редизайн вычитки договора, 404, разметка, перелинковка

### What was done
Работа велась командами (составы, варианты и решения — `deliverables/TEAM-RUN-3.md`).

- **Редизайн блока «вычитка договора»** в статье «ИИ для юристов» (замечание владельца о «портянке находок»):
  было — все 6 находок раскрыты сразу, правая колонка в 1,74 раза тяжелее документа, главная находка
  («чего ИИ не увидел») внизу за кнопкой. Стало — реестр: уровень риска + короткий заголовок, пояснение
  раскрывается по клику, открыто не больше одной находки, первая находка высокого риска раскрывается сама,
  добавлена сводка «5 находок: 3 высокий риск · 2 проверить». Видно текста до клика: 928 → 245 символов.
  Клик по подсвеченному пункту в договоре раскрывает соответствующую строку реестра. Без JS — статичный
  список с раскрытыми пояснениями, кнопка не притворяется кликабельной. `tools/kit-check.mjs` расширен:
  проверяет раскрытие, `aria-expanded`, «одно открытое за раз», сводку.
- **Страница 404**: `src/pages/404.astro` (4 ссылки-маршрута, `noindex, follow`), `ErrorDocument 404 /404.html`
  в `.htaccess`, новый проп `noindex` в `Base.astro`. Исключена из `sitemap.xml` и из проверок индексации.
- **Микроразметка на 6 страницах:** главная — `Organization` + `WebSite`; услуги, AI-хаб и 3 кейса —
  `BreadcrumbList`. Цены и даты в схему не добавляли: нужны подтверждения владельца.
- **Перелинковка:** три статьи-сироты («ИИ для HR», «ИИ для медцентра», «ИИ в недвижимости») получили
  по 2 входящие ссылки из тематически близких материалов.
- **Заголовки:** применены 9 укороченных формулировок (38–52 символа вместо 62–80), описание статьи
  «Что такое ИИ-агент» сокращено с 186 до 109 символов.
- **Инструменты:** `seo-audit` больше не считает служебную 404 в карте сайта; `make-sitemap` её исключает;
  `content-edits.json` дополнен до 24 записей (title/meta/links/classes/jsonld).
- **Проверки:** `npm run check` — сверка OK, JS без ошибок, интерактив OK, QA 0 предупреждений,
  SEO-аудит **0 ошибок и 0 предупреждений** (впервые). Контент-аудит: расхождений по времени чтения нет.

### Найденные при самопроверке ошибки (исправлены в этом же прогоне)
- Блок `jsonLd` в 6 файлах попал за пределы frontmatter → сборка падала. Исправлено, лишние `---` убраны
  из тела страниц.
- `seo-audit` сначала требовал 404 в `sitemap.xml` — логика перевёрнута на правильную.

### Next steps
- Бэклог: каннибализация «продажи ↔ ИИ-агент», переписывание 19 однородных статей на `Article.astro`,
  шрифты своего хостинга, `Service`/`Offer` и `Article` на кейсах (нужны подтверждения).
- Блокеры: отзыв токена Telegram-бота, доступы Beget, прод-выгрузка.

## Прогон 2026-09-25 (раунд 4, тот же день): карточка автора на всех статьях блога

Запрос владельца: «добавь ещё эту секцию — она была на старом сайте в статьях» + скриншот карточки
(АВТОР / Олег Кречетов / био / TELEGRAM @olegkrechetov). Подробный разбор команд — `deliverables/TEAM-RUN-4.md`.

- **Новый компонент** `site/src/components/blog/AuthorCard.astro` — подпись, имя, био, кнопка Telegram.
  Данные из `AUTHOR`/`CONTACTS` (`src/data/site.ts`), стили внутри компонента на явных значениях, чтобы
  один вид работал и на светлом «листе», и в тёмной теме старых статей.
- **Все 19 «старых» статей переведены на компонент**: рукописные `<aside class="article-aside-right">`
  (2 копии текста автора в каждой) заменены на `<AuthorCard />`; в `styles.css` удалено 63 строки мёртвых
  `.sticky-*` и `.contact-row`, в `article.css` — 1 264 символа дублирующих стилей карточки.
- **Карточка добавлена в два новых макета**: `ii-dlya-yuristov` (через `Article.astro`, `.bl-author`) и
  `chto-takoe-ii-agent` (свой макет). Итого карточка у **21 из 21** статьи; копий био в разметке — 0.
- **Мобильные**: правило `≤1179px` больше не скрывает карточку (скрывается только содержание), карточка
  видна на 390 / 768 / 1024 / 1180 / 1440 px, ширина до 520 px. Горизонтального скролла нет на всех ширинах.
- **Колонки листа** пересчитаны под карточку: 200 / 700 / 240 px (было 220 / 700 / 208 при зазоре 56).
- **Проверка в инструментах**: `tools/qa-check.py` теперь требует карточку в каждой статье, ровно одну,
  с текстом из `site.ts` и ссылкой на Telegram, и следит, чтобы старая разметка `sticky-author` не вернулась.
- **Стили в сборке**: один общий файл `_astro/AuthorCard.*.css` (1 848 байт) подключён ко всем 21 статье.
- **Проверки**: `npm run check` зелёный (сверка OK, JS без ошибок, интерактив OK, QA 0 предупреждений,
  SEO-аудит 0/0). `tools/content-edits.json` дополнен 13 пояснениями «карточка автора вынесена в компонент».
- **Отклонено осознанно**: светлая карточка вместо тёмной, сохранение 19 копий разметки, скрытие карточки
  на мобильных, перенос карточки в начало статьи. Подпись-обещание от имени владельца не добавляли —
  её нет на присланном скриншоте.
- **Скриншоты для владельца**: `deliverables/screenshots/author-mobile-before-after.png` (телефон, было/стало)
  и `author-desktop-both.png` (один компонент в двух макетах).
### Тот же прогон, правка по замечанию владельца: «тёмный цвет как-то не очень»

- Карточка получила **две темы одним компонентом** (проп `theme`, значения через переменные `--ac-*`):
  на светлом «листе» (`Article.astro`, `chto-takoe-ii-agent`) — `theme="light"` (белая поверхность,
  тонкая линия, подпись `--acc-t` #0A7EA4, кнопка Telegram на голубой подложке `--acc-soft`),
  в 19 тёмных статьях — тёмная, как была.
- Контрасты светлой темы проверены расчётом: подпись 4.63:1, био 6.41:1, текст кнопки 17.2:1 — WCAG AA и выше.
- QA: добавлено правило — на светлом листе карточка обязана быть светлой, на тёмной странице тёмной
  (ловит именно тот случай, когда тема задана «не там», как было при первой попытке через переменные обёртки).
- Варианты светлой карточки показаны владельцу: A. белая (стоит сейчас), B. бумажная `--paper2`, C. белая с голубой полосой.
  Скриншот: `deliverables/screenshots/author-light-variants.png`.

## Прогон 2026-09-25 (вечер): ревизия SEO-стратегии — «насколько продуман захват органики»

Владелец вернул фокус на важные цели. Разбор по замерам — `deliverables/SEO-STRATEGY-REVIEW.md`.

Главные находки (проверено на живом домене и в сборке):
- **Прод отстаёт от репозитория минимум на два раунда.** На smartsolutions.today: `sitemap.xml` — 25 адресов
  (в сборке 28), новая статья «Что такое ИИ-агент» отдаёт **404**, `/404.html` — **404** (ошибки отдаёт Apache),
  `robots.txt` старой версии (без `Clean-param` и разрешения ИИ-ботам), заголовок статьи о продажах —
  дораундовый. Деплой ручной; GitHub Action только собирает и проверяет, выкладки нет.
- **Измерительного контура нет:** цели Метрики не настроены (клики в Telegram/телефон не считаются),
  переобхода страниц в Вебмастере не было, позиции ни разу не замерялись.
- **Воронка в деньги разорвана:** из тел 21 статьи — **0 контекстных ссылок** на `/ai.html`, `/services.html`
  и кейсы (внутри блога при этом 63 ссылки статья↔статья). Кейсы — почти сироты: по 1 входящей, и та с главной.
- **Денежных страниц 2 на 21 статью.** Крупнейший профильный кластер «ИИ-агенты» (115 459 показов/мес,
  25 запросов) закрыт одной информационной статьёй; нет страниц под «автоматизацию продаж» (2 054),
  «ИИ для сайта» (16 213), отраслевые посадочные.
- **E-E-A-T:** нет страницы «О компании», автора, реквизитов юрлица и политики обработки данных в сборке.
- Техника: `styles.css` 86,9 КБ render-blocking на каждой странице, 10 шрифтов Google без `preload`.

Честная самокритика в отчёте: раунды 3–4 ушли на полировку (заголовки, разметка, карточка автора), тогда как
порядок должен был быть «выложить → переобойти → настроить замер → потом полировать».

План разбит на P0 (выкладка + переобход + 4 цели Метрики + baseline позиций), P1 (4 денежные страницы
под головные запросы + перелинковка статьи → посадочные), P2 (E-E-A-T, обновление 19 статей, кейсы),
P3 (верх воронки, внешние сигналы, хабы). У каждой фазы — команда из 10–14 ролей с критиком и проверяющим,
приёмка в цифрах, и список из 8 решений, которые нужны от владельца (доступы, Метрика, реквизиты, кейсы, цены).

## Прогон 2026-09-25 (вечер, шаг 2): воронка «статьи → деньги» и релизная инфраструктура

Решение владельца: **релиз позже, одним пакетом** — сначала этапы. План и границы — `deliverables/RELEASE-PLAN.md`.

- **Воронка закрыта (главная находка ревизии).** Было: из 21 статьи в тело статьи **не вело ни одной ссылки**
  на `/ai.html`, `/services.html` и кейсы. Стало: 21 из 21 статьи ведут в деньги, 25 контекстных ссылок в зоне контента (у 4 статей — по две:
  8 разных анкоров; кейсы упомянуты из 4 тематически близких статей). Текст абзаца у каждой статьи свой —
  шаблонного «подробнее тут» нет.
- **Правило в QA:** `tools/qa-check.py` теперь считает контекстные ссылки только в зоне контента
  (от `<h1>` до блока контактов) — так не засчитываются шапка, мобильное меню вне `<nav>`, хлебные крошки,
  CTA и подвал. Правило проверено «на регресс»: удаление ссылки у одной статьи ловится.
- **Релизная инфраструктура:**
  - `site/release.sh` — релиз одной командой: `npm run check` → rsync → проверка прода → IndexNow → чек-лист
    ручных шагов (Вебмастер, цели Метрики). Есть `--dry-run` и `--skip-rsync`.
  - `site/tools/verify-prod.py` — проверяет все URL из `sitemap.xml` на проде: код ответа и совпадение
    `<title>` со сборкой (ловит «прод отстал»). Из песочницы честно сообщает, что сеть недоступна.
  - `site/tools/indexnow.mjs` + ключ `public/02ffa2c8…txt` — уведомление Яндекса и Bing о 28 URL
    в момент релиза (Google IndexNow не поддерживает: для него sitemap + Search Console).
- **Проверки:** `npm run check` зелёный (сверка OK, JS без ошибок, интерактив OK, QA 0, SEO-аудит 0/0).
  `content-edits.json` — 28 записей, причина добавлена к 21 статье.

### Важно: восстановление состояния git (моя ошибка, исправлена)
Локальный `.git` в песочнице оказался на базовом коммите `88bec6a`, а работа раундов 2–4 лежала
в рабочем дереве незакоммиченной (коммиты при этом были на remote — я их пушил). Я выполнил
`git checkout tools/content-edits.json` и затёр накопленные 28 записей сверки до 9.
Восстановлено: `git fetch` → `git reset --mixed FETCH_HEAD` (рабочее дерево сохранено) → `content-edits.json`
взят из remote-типа `2597fc8`. Локальный HEAD теперь `2597fc8` + правки этого шага.
**Урок:** перед `git checkout` файла — убедиться, что коммит локально существует; в этой песочнице
`.git` может откатываться, а источник истины — remote.

## Прогон 2026-09-25 (вечер, шаг 3): E2, страница № 1 — посадочная «ИИ-агенты»

Командный разбор — `deliverables/TEAM-RUN-5.md`.

- **Новая страница** `src/pages/ai-agenty.astro` → `/ai-agenty.html` (10 разделов, 1 684 слова).
  Закрывает крупнейшую профильную дыру: кластер «ИИ-агенты» — 115 459 показов/мес, 25 запросов
  (`ии агент` 72 294, `ai агент` 11 385, `создание ии агентов` 3 660, `ии агент для 1с` 526),
  до этого была только информационная статья.
- **Обещания и цены не выдуманы:** аудит бесплатный, пилот 2–4 недели, «точную цифру называем после
  аудита» — всё повторяет согласованное владельцем на `/ai.html`. Вилок «от __» нет.
- **Честные блоки, которые обычно прячут:** интеграции (не обещаем «подключим к любой 1С»),
  границы «где агент не нужен» (АСУ ТП — не наш профиль, требование 100% гарантии, отсутствие данных).
- **Разметка:** `Service` + `FAQPage` + `BreadcrumbList`; FAQ — один источник (массив → видимый блок и схема).
- **Перелинковка:** `/ai.html` → посадочная; 4 статьи → посадочная; посадочная → 6 статей.
- **Карта сайта:** `make-sitemap` дополнен правилом и датой → 29 URL (было 28); IndexNow сухой прогон — 29 URL.
- **Замеры:** 390–1440 px без горизонтального скролла, 1 H1 / 11 H2, FAQ работает по одному,
  title 45 симв., description 145, `npm run check` зелёный (QA 0, SEO 0/0).
- **Ограничения (честно):** цен нет (нечем называть), кейсов по ИИ нет (три существующих — про привлечение
  клиентов, притягивать их к агентам не стали), тап-зоны подвала 22 px — дефект всего сайта, оставлен в E3.
- **Дальше по E2:** нужны решения владельца по трём страницам — интенты «автоматизации продаж»,
  продукт «ассистент для сайта» (чат = новый продукт), использование интерактива с договором для юристов.


---

## Шаг 8. Написанные статьи: интерактив в старом макете (2026-09-25)

Вопрос владельца: «а с написанными статьями мы будем что-то делать?» Ответ — рабочим образцом, а не планом:
19 статей старого макета имели **0 интерактивов**; теперь интерактив есть в 5 самых востребованных,
в статье о продажах появился главный интерактив статьи.

- **Тёмный вариант кита:** статьи старого макета не подключают `article.css`, а `kit.css` рассчитан на светлый лист.
  Решение — обёртка `.kit-dark` в `kit.css` (переопределяет переменные), а не перекраска статей: светлую тему
  владелец не выбирал. Заодно подняты контрасты подписей блока (было 3,3 — ниже нормы AA) в обеих темах.
- **Калькулятор эффекта** в 5 статьях (`ii-dlya-prodazh`, `vnedrenie-ii-v-biznes`, `ii-dlya-hr`,
  `ii-dlya-buhgalterii`, `ii-dlya-marketinga`): «пример, а не наши данные», у каждой — оговорка, чего не считает.
- **Новый компонент `Funnel.astro`** (воронка «заявки → диалог → сделка») — главный интерактив статьи о продажах.
  Модель на допущениях читателя, а не на выдуманной статистике; при скорости ответа 5 минут честно показывает,
  что дальше улучшать почти нечего. Без JS цифры видны (расчёт на сервере).
- **Автопроверка:** в `tools/kit-check.mjs` добавлен тест `funnel` (нашёл и позволил исправить реальный баг —
  подпись «ответ 60 мин» не обновлялась при движении ползунка).
- **Замеры:** `npm run check` зелёный (интерактив OK на 6 страницах, QA 0, SEO 0/0); формулы сошлись с ручным
  счётом (16 / 52 / 6 / 13,3 / 17,5 ч/мес; воронка 75,5 / 40 / 20 сделок); контрасты 8,6–17,7; 390 px без overflow;
  нумерация рисунков без дублей; вес статьи +6 КБ при общих `kit.css` 13,8 КБ и `kit.js` 9,5 КБ.
- **Что осталось решить:** масштаб (варианты A–D в `RELEASE-PLAN.md`, раздел E2.5), реальные кейсы,
  доступ к Вебмастеру, судьба слабых тем. Разбор команды — `deliverables/TEAM-RUN-6.md`.

---

## Шаг 9. Волна B: две статьи доведены до эталона (2026-09-25)

Решение владельца: **«B → потом A»** — волна по спросу, затем полная переработка остальных.

- **«ИИ для продаж»** (4 585 показов/мес): H1 под два запроса («ии для продаж» + «автоматизация продаж»),
  убрано обещание «в 4 раза»; вкладки «вручную / с ИИ» (Рис. 3); таблица «что забирает ИИ, а что остаётся
  менеджеру»; промпт для черновика ответа; раздел о пилоте переписан под один канал (шаблонная формулировка
  стояла в 13 статьях); FAQ 5 → 7 (Битрикс24/1С — без обещания «подключим к любой CRM»; старт без CRM).
- **«Внедрение ИИ в бизнес»** (pillar, 27 485 показов/мес): новый компонент `Checklist.astro` — 10 пунктов
  готовности дают шкалу и честный вердикт («пока рано» / «можно пилот» / «есть база для масштабирования»);
  таблица «как проверить критерий у себя за день»; FAQ 5 → 7 (данные, свои серверы и российские модели).
- **Библиотека:** в kit добавлены `Checklist` и универсальная таблица (тёмная внутри `.kit-dark`),
  подсветка активного уровня в чек-листе; контрасты таблицы 8,9–15,0.
- **Автопроверка:** в `kit-check.mjs` добавлен тест `check` (вердикт меняется, шкала доходит до 100 %).
  Прогон: продажи — `tabs 2 · calc 16→120 · funnel 40→5 · prompt`; внедрение — `calc 52→433 · check 0→10`;
  `npm run check` зелёный (интерактив OK на 6 стр., QA 0, SEO 0/0).
- **Замер по блогу:** интерактив в 6 статьях из 21 (было 0 в 19 legacy), таблицы в 3 (было 1).
- **Дальше в волне B:** строительство (3 305), логистика (1 923), GEO (1 454, смена главного запроса),
  HR (1 169). Разбор шага — `deliverables/TEAM-RUN-7.md`.

---

## Шаг 10. Волна B закрыта: 4 статьи + GEO со сменой интента (2026-09-25)

Запрос владельца: «делай как лучше как профессиональнее и гео тоже».

- **ИИ в строительстве:** сканер сметы и договора подряда (5 находок + карточка «чего ИИ не увидел»),
  промпт для проверки чужой сметы, калькулятор часов сметчика; таблица-md собрана в настоящую.
- **Автоматизация логистики:** таймлайн «путь заявки» с ролями ИИ/человек на каждом этапе, калькулятор часов логиста.
- **GEO → «Продвижение в нейросетях»:** интент сменён (H1, title, description, headline), **убрана
  недоказанная цифра «15–20 % запросов по оценке аналитиков»**, главный интерактив `Sources` показывает
  происхождение каждой фразы ответа и отдельно — «догадку модели»; шаблон цитируемого блока; таблица SEO ↔ GEO.
- **ИИ для HR и кадров:** таймлайн «путь кандидата», таблица «что ИИ оценивает — и что не должен видеть»
  (ст. 3 ТК РФ), FAQ про права кандидатов и хранение резюме.
- **Системный дефект исправлен:** в 4 статьях (строительство, логистика, маркетинг, недвижимость)
  таблицы были markdown-строками внутри абзацев и показывались читателю текстом с палками — теперь настоящие таблицы кита.
- **Библиотека:** `Timeline.astro`, `Sources.astro`; таблица вынесена в kit.css (тёмная внутри `.kit-dark`).
- **Автотесты:** `timeline` (переключение этапов, aria, пустые описания) и `sources` (подсветка фраз источника,
  снятие по повторному клику, наличие фразы без источника). `npm run check` зелёный, интерактив OK на 9 страницах.
- **Замер по блогу:** интерактив в 9 статьях из 21, таблиц 11, у шести статей по 7 вопросов FAQ (было «ровно 5» у 18 из 20).
- Разбор — `deliverables/TEAM-RUN-8.md`. Дальше — этап A (12 оставшихся статей), порядок по воронке.

---

## Шаг 11. Все статьи — на согласованный макет (2026-09-25)

Запрос владельца: «почему все статьи разные цветов — мы же согласовали новый стиль».

- **19 тёмных статей переведены на светлый лист** `layouts/Article.astro`; две статьи, уже бывшие на нём
  (`chto-takoe-ii-agent`, `ii-dlya-yuristov`), не трогались. Итог: **21 статья из 21** — один макет, один лист,
  один набор компонентов.
- **Восстановлены реальные потери конвертера:** абзац-воронка «Как устроен наш пилот…» (турагентство и др.),
  абзац «Что происходит после расчёта… → /ai.html» (как посчитать эффект: он же денежная ссылка для qa-check),
  формула расчёта в блоке «Коротко».
- **Починены SEO-мелочи переноса:** авторские `og:title`/`og:description` теперь переносятся (недвижимость,
  как посчитать эффект); JSON-LD собирается макетом из H1, description и видимого FAQ.
- **Светлый лист перестал «слепить»:** legacy-блоки статьи о внедрении (карточки недель пилота, «где не
  окупится», выносная цитата, карточка формулы) получили светлую палитру через алиасы токенов; подписи
  внутри SVG-схем перекрашены в тёмные чернила (замер: страниц с контрастом текста в SVG ниже 3:1 —
  было 6, стало 0); герой-арт перестал накладывать подписи.
- **`verify.py` снова зелёный:** 13 страниц с намеренными правками зарегистрированы в `content-edits.json`
  (`allow: id/jsonld` + причина).
- **Замеры:** `npm run check` — exit 0 (build 30 стр., verify «OK — расхождений нет», 0 битых ссылок,
  JS без ошибок, интерактив OK на 20 стр., QA OK, SEO OK). Аудит сохранности старого текста ↔ сборка:
  0 потерянных блоков. Браузерный замер 21 статьи: светлый лист у всех, контраст 18.25:1, мобильный
  скролл 0 px, все компоненты реагируют на клик.
- Разбор — `deliverables/TEAM-RUN-9.md`. Скриншоты прогона не сохранены: песочница сбросила `/tmp`
  (Chromium), CDN браузера недоступен.
- Дальше по плану — этап A (12 статей, порядок по воронке) и решения владельца из RELEASE-PLAN §5.
