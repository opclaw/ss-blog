# CLAUDE.md — Smart Solutions Site

> Локальные инструкции проекта. Дополняют глобальные правила из `~/.claude/CLAUDE.md`.
> **Активная версия сайта:** `version 4/`. Все изменения вносим только туда, если явно не сказано иначе.
> Последнее обновление: 2026-08-24.

---

## 1. Структура проекта

```
/
├── CLAUDE.md              <- этот файл
├── SESSION.md             <- журнал сессий (append-only)
├── MODEL-ROUTING.md       <- какая модель за что отвечает (обновляется по тестам)
├── CONTENT-MAP.md         <- SEO-карта тем × Wordstat (по кластерам)
├── version 1/             <- самый ранний прототип (Next.js → HTML)
├── version 2/             <- премиум-редизайн, визуальные эффекты
├── version 3/             <- полная переработка (устаревшая, не поддерживается)
├── version 4/             <- АКТИВНАЯ ВЕРСИЯ (деплой)
│   ├── index.html         <- главный лендинг
│   ├── services.html      <- услуги и цены
│   ├── ai.html            <- хаб AI-направления (с блоком блога)
│   ├── cases/             <- детальные кейсы (3 шт)
│   ├── blog/              <- блог / AI-направления / исследования
│   │   ├── index.html     <- оглавление блога
│   │   └── *.html         <- статьи (19 шт)
│   ├── styles.css
│   ├── script.js
│   ├── icons.svg
│   ├── robots.txt
│   ├── sitemap.xml
│   ├── .htaccess
│   ├── google*.html, yandex_*.html  <- верификации поисковиков
│   └── send.php           <- НЕ ИСПОЛЬЗУЕТСЯ (форма убрана)
└── .git
```

### Правила работы со структурой
- **Все правки сайта — только в `version 4/`**.
- `version 1/`, `version 2/`, `version 3/` — исторические артефакты, не трогать без явной просьбы.
- Деплой идёт из `version 4/` (Vercel preview → Beget prod, rsync).
- `send.php` оставлен в репозитории, но **не подключён** (форма убрана по решению владельца).

---

## 2. Бизнес-контекст

**Позиционирование:** Smart Solutions — системы привлечения клиентов. Не «разработка сайтов».

**Продукты:**
1. Проект под ключ — от 200 000 ₽.
2. Сопровождение — два пакета (базовое от 50 000 ₽/мес, расширенное от 100 000 ₽/мес).
3. Партнёрство — упоминается мягко (не отдельный продукт).

**Не меняется без согласования:**
- Цены «от ___», не фиксированные.
- «Ретейнер» → «Сопровождение».
- Партнёрство — не продукт.
- Главный чек: 700 000–1 000 000 ₽.
- Гарантия: «остаёмся рядом».

**Раздел AI-направления (новое, 2026):**
- Направление: внедрение ИИ с замером эффекта (аудит → пилот → боевая система).
- Ценовая вилка пилотов: 100–300К, фиксируется после аудита.
- CTA — только мессенджеры (Telegram/WhatsApp/телефон/почта), формы нет.
- Голос: прямой, анти-хайп, измеримость в часах и рублях.

---

## 3. Технический стек

- **Фронт:** чистый HTML + CSS + Vanilla JS (без фреймворков, без сборки).
- **Аналитика:** не установлена (отказ владельца, в т.ч. cookie-баннеры).
- **SEO:** `robots.txt`, `sitemap.xml`, Schema.org (Article + FAQPage + CollectionPage), `.htaccess`, canonical на всех страницах.
- **Деплой:** Vercel (preview `ss-ai-preview.vercel.app`) → Beget (prod, rsync).
- **Графика:** SVG-фигуры рисуются вручную в статьях (2–3 шт/статья, данные из текста).
- **CSS-токены:** переменные в `:root` (цвета, шрифты, отступы), бан-лист идей в голове (нет cyan/purple, нет эмодзи, нет курсива).

### Блог: правила
- Статья = H1, лид, 10–12 H2, FAQ×5, TL;DR, 2–3 SVG-фигуры, CTA.
- Автор: «Олег Кречетов, основатель Smart Solutions» + Schema Person.
- Schema: Article + FAQPage + Person.
- Запреты в тексте: «революция», «магия», «просто посадить», неподтверждённые цифры без пометки «оценка/пример».

---

## 4. Workflow сессий

### В начале каждой сессии
1. Прочитать этот `CLAUDE.md`.
2. Прочитать `SESSION.md`.
3. Прочитать `MODEL-ROUTING.md` (чтобы знать, какую модель на какую задачу ставить).
4. Проверить `git status` и текущую ветку.
5. Убедиться, что работаем в `version 4/`.

### Во время работы
1. После каждого значимого шага обновлять `SESSION.md` (append, не overwrite).
2. Перед коммитом проверять, что изменения затрагивают только нужные файлы.
3. **Не коммитить `.claude/`, `.DS_Store`, `.vercel/`** — `version 4/.gitignore` уже настроен.
4. Коммиты — conventional commits: `feat:`, `fix:`, `content:`, `style:`, `seo:`, `deploy:`, `docs:`, `chore:`.

### В конце сессии
1. Обновить `SESSION.md`: что сделано, что не доделано, следующие шаги.
2. Если были открытия — обновить `MODEL-ROUTING.md` и/или сохранить в skills.

---

## 5. Запрещённые действия без согласования

- Менять позиционирование, продукты или цены.
- Переключать активную версию с `version 4/`.
- Удалять или переписывать `version 1/2/3/`.
- Подключать `send.php` или другие формы (отказ зафиксирован).
- Устанавливать Яндекс.Метрику или cookie-баннер (отказ зафиксирован).
- Менять навигацию главной страницы без согласования.

---

## 6. Текущие приоритеты

1. **Запуск в прод** — мердж `ai-direction` → `main` → rsync на Beget (ждёт Beget-доступы).
2. Регистрация в Яндекс.Вебмастере + переобход новых страниц.
3. Публикация со склада (19 статей) по 2–3/неделю.
4. Мониторинг позиций через 4–6 недель.

---

*История изменений: 2026-06-30 (исходная) → 2026-08-24 (синхронизация с реальностью version 4: добавлен AI-раздел и блог; убраны форма/Метрика/consent/privacy; добавлены MODEL-ROUTING/CONTENT-MAP/SESSION-правила).*